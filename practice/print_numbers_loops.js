// print number from 1 to 10 using loops

for (let i = 1; i <= 10; i++) {
    const element = i;

    console.log(element);
}

// print table of any number

for (let i = 1; i <= 10; i++) {
    let number = 2;
    const element = number*i;
    console.log(`2*${i} = ${element}`);
    
}